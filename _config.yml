# Site's title
title: Selene Archive

# Disable automatic theming
theme: null

# Output directory for Jekyll site generation
destination: .tmp

# Additional subdirectories to include
include:
  - _pages
defaults:
  -
    scope:
      type: pages
      path: _pages
    values:
      is_post: false
      layout: default
